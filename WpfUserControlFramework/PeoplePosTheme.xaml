<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RestaurantPosWpf">

    <!-- ===== Converters ===== -->
    <!-- UI Scaling converters (bind to UiScaleState.FontScale) -->
    <local:FontScaleConverter x:Key="FontScaleConverter" />
    <local:LayoutScaleConverter x:Key="LayoutScaleConverter" />
    <local:ThicknessScaleConverter x:Key="ThicknessScaleConverter" />
    <local:ScaledDoubleConverter x:Key="ScaledDoubleConverter" />
    <local:CornerRadiusFromHeightConverter x:Key="CornerRadiusFromHeightConverter" />
    <local:WidthMultiplierConverter x:Key="WidthMultiplierConverter" />
    <local:WidthToHeightRatioConverter x:Key="WidthToHeightRatioConverter" />

    <!-- Visibility converters -->
    <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter" />

    <!-- Text / data converters -->
    <local:BadgeListConverter x:Key="BadgeListConverter" />
    <local:ContactSplitterConverter x:Key="ContactSplitterConverter" />
    <local:CountToTextConverter x:Key="CountToTextConverter" />
    <local:IsNullOrWhiteConverter x:Key="IsNullOrWhiteConverter" />
    <local:LastIndexConverter x:Key="LastIndexConverter" />

    <!-- Color / brush converters -->
    <local:StringToBrushConverter x:Key="StringToBrushConverter" />
    <local:TrendColorConverter x:Key="TrendColorConverter" />
    <local:StatusToBrushConverter x:Key="StatusToBrushConverter" />
    <local:StatusToBorderConverter x:Key="StatusToBorderConverter" />
    <local:StatusToTextBrushConverter x:Key="StatusToTextBrushConverter" />

    <!-- Layout converters -->
    <local:VersionGridMaxHeightConverter x:Key="VersionGridMaxHeightConverter" />

    <!-- ===== UiScaleState binding source ===== -->
    <local:UiScaleState x:Key="UiScaleState" />

    <!-- ===== Colors ===== -->
    <Color x:Key="MainForegroundColor">#111827</Color>
    <Color x:Key="DimmedForegroundColor">#687280</Color>
    <Color x:Key="FocusInnerColor">#93C5FD</Color>
    <Color x:Key="FocusOuterColor">#C1DEFE</Color>

    <!-- Category badge colors -->
    <Color x:Key="ComplianceBadgeColor">#DBEAFE</Color>
    <Color x:Key="ComplianceBadgeTextColor">#1E40AF</Color>
    <Color x:Key="LegalBadgeColor">#FEF3C7</Color>
    <Color x:Key="LegalBadgeTextColor">#92400E</Color>
    <Color x:Key="BankingBadgeColor">#D1FAE5</Color>
    <Color x:Key="BankingBadgeTextColor">#065F46</Color>

    <!-- ===== Brushes ===== -->
    <SolidColorBrush x:Key="MainForeground" Color="{StaticResource MainForegroundColor}" />
    <SolidColorBrush x:Key="DimmedForeground" Color="{StaticResource DimmedForegroundColor}" />
    <SolidColorBrush x:Key="FocusInner" Color="{StaticResource FocusInnerColor}" />
    <SolidColorBrush x:Key="FocusOuter" Color="{StaticResource FocusOuterColor}" />

    <SolidColorBrush x:Key="ComplianceBadgeBrush" Color="{StaticResource ComplianceBadgeColor}" />
    <SolidColorBrush x:Key="ComplianceBadgeTextBrush" Color="{StaticResource ComplianceBadgeTextColor}" />
    <SolidColorBrush x:Key="LegalBadgeBrush" Color="{StaticResource LegalBadgeColor}" />
    <SolidColorBrush x:Key="LegalBadgeTextBrush" Color="{StaticResource LegalBadgeTextColor}" />
    <SolidColorBrush x:Key="BankingBadgeBrush" Color="{StaticResource BankingBadgeColor}" />
    <SolidColorBrush x:Key="BankingBadgeTextBrush" Color="{StaticResource BankingBadgeTextColor}" />

    <!-- Primary button colors -->
    <SolidColorBrush x:Key="PrimaryButtonBackground" Color="#2563EB" />
    <SolidColorBrush x:Key="PrimaryButtonForeground" Color="White" />
    <SolidColorBrush x:Key="PrimaryButtonHoverBackground" Color="#1D4ED8" />
    <SolidColorBrush x:Key="PrimaryButtonPressedBackground" Color="#1E40AF" />

    <!-- Secondary button colors -->
    <SolidColorBrush x:Key="SecondaryButtonBackground" Color="White" />
    <SolidColorBrush x:Key="SecondaryButtonForeground" Color="{StaticResource MainForegroundColor}" />
    <SolidColorBrush x:Key="SecondaryButtonBorder" Color="#D1D5DB" />
    <SolidColorBrush x:Key="SecondaryButtonHoverBackground" Color="#F9FAFB" />

    <!-- TextBox / ComboBox colors -->
    <SolidColorBrush x:Key="InputBorderBrush" Color="#D1D5DB" />
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="White" />
    <SolidColorBrush x:Key="InputFocusBorderBrush" Color="#93C5FD" />

    <!-- ===== Focus Ring Template ===== -->
    <ControlTemplate x:Key="FocusRingTemplate" TargetType="Control">
        <Grid>
            <Border BorderBrush="{StaticResource FocusOuter}"
                    BorderThickness="3"
                    CornerRadius="6"
                    Margin="-3" />
            <Border BorderBrush="{StaticResource FocusInner}"
                    BorderThickness="2"
                    CornerRadius="5"
                    Margin="-1" />
        </Grid>
    </ControlTemplate>

    <!-- ===== VcPrimaryButtonStyle ===== -->
    <Style x:Key="VcPrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryButtonBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryButtonForeground}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="BackgroundBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <!-- Focus ring -->
                        <Border x:Name="FocusOuterRing"
                                BorderBrush="{StaticResource FocusOuter}"
                                BorderThickness="3"
                                CornerRadius="8"
                                Margin="-2"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusInnerRing"
                                BorderBrush="{StaticResource FocusInner}"
                                BorderThickness="2"
                                CornerRadius="7"
                                Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background"
                                    Value="{StaticResource PrimaryButtonHoverBackground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background"
                                    Value="{StaticResource PrimaryButtonPressedBackground}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusOuterRing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="FocusInnerRing" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== VcSecondaryButtonStyle ===== -->
    <Style x:Key="VcSecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SecondaryButtonBackground}" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryButtonForeground}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource SecondaryButtonBorder}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="BackgroundBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <!-- Focus ring -->
                        <Border x:Name="FocusOuterRing"
                                BorderBrush="{StaticResource FocusOuter}"
                                BorderThickness="3"
                                CornerRadius="8"
                                Margin="-2"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusInnerRing"
                                BorderBrush="{StaticResource FocusInner}"
                                BorderThickness="2"
                                CornerRadius="7"
                                Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background"
                                    Value="{StaticResource SecondaryButtonHoverBackground}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusOuterRing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="FocusInnerRing" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== IcBaseTextBoxStyle ===== -->
    <Style x:Key="IcBaseTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource MainForeground}" />
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <Border x:Name="FocusOuterRing"
                                BorderBrush="{StaticResource FocusOuter}"
                                BorderThickness="3"
                                CornerRadius="8"
                                Margin="-2"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusInnerRing"
                                BorderBrush="{StaticResource FocusInner}"
                                BorderThickness="2"
                                CornerRadius="7"
                                Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush"
                                    Value="{StaticResource InputFocusBorderBrush}" />
                            <Setter TargetName="FocusOuterRing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="FocusInnerRing" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== IcEnabledTextBoxStyle ===== -->
    <Style x:Key="IcEnabledTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource IcBaseTextBoxStyle}">
        <Setter Property="IsReadOnly" Value="False" />
    </Style>

    <!-- ===== ComboBox toggle button (internal) ===== -->
    <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="20" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Border"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" />
                        <Border Grid.Column="0"
                                Background="Transparent"
                                Margin="1" />
                        <Path x:Name="Arrow"
                              Grid.Column="1"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Data="M0,0 L4,4 L8,0"
                              Stroke="{StaticResource DimmedForeground}"
                              StrokeThickness="1.5"
                              Fill="Transparent" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== IcBaseComboBoxStyle ===== -->
    <Style x:Key="IcBaseComboBoxStyle" TargetType="ComboBox">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource MainForeground}" />
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Style="{StaticResource ComboBoxToggleButtonStyle}"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False"
                                      ClickMode="Press" />
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="Left" />
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="White"
                                        BorderBrush="{StaticResource InputBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,2,0,0">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                        <!-- Focus ring -->
                        <Border x:Name="FocusOuterRing"
                                BorderBrush="{StaticResource FocusOuter}"
                                BorderThickness="3"
                                CornerRadius="8"
                                Margin="-2"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusInnerRing"
                                BorderBrush="{StaticResource FocusInner}"
                                BorderThickness="2"
                                CornerRadius="7"
                                Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="ToggleButton" Property="BorderBrush"
                                    Value="{StaticResource InputFocusBorderBrush}" />
                            <Setter TargetName="FocusOuterRing" Property="Visibility" Value="Visible" />
                            <Setter TargetName="FocusInnerRing" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== IcEnabledComboBoxStyle ===== -->
    <Style x:Key="IcEnabledComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource IcBaseComboBoxStyle}">
        <Setter Property="IsEnabled" Value="True" />
    </Style>

</ResourceDictionary>
